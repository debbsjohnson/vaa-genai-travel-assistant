# pytest.ini
[pytest]
# only look for tests in the `tests/` directory
testpaths = tests
# only treat files matching test_*.py or *_test.py in tests/ as tests
python_files = test_*.py *_test.py
# don’t recurse into scripts/
norecursedirs = scripts